import Reveal from "./Reveal"; import SectionHeading from "./SectionHeading"; import {achievements} from "../data/profile";
export default function Achievements(){return <section className="section-light py-24 md:py-32"><div className="max-w-[1240px] mx-auto px-5 md:px-8"><Reveal><SectionHeading index="03 / Impact" title="Proof, not just promises." intro="A snapshot of measurable outcomes from performance, team growth and operational projects."/></Reveal>
 <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-4">{achievements.map((a,i)=><Reveal key={a.label} delay={i*.05}><div className={`rounded-3xl p-7 h-full border ${i===0?"bg-[#091019] text-white border-[#091019]":"bg-white border-black/5"} card-hover`}><p className={`display text-5xl ${i===0?"text-[#4de1ff]":"text-[#091019]"}`}>{a.stat}</p><h3 className="font-semibold mt-5">{a.label}</h3><p className={`text-sm leading-relaxed mt-2 ${i===0?"text-white/55":"text-[#091019]/55"}`}>{a.detail}</p></div></Reveal>)}</div></div></section>}
